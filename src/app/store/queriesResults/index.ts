import { StoreAction } from "../docs";

export const SAVE_NEW_RESULTS_ACTION = "SAVE_NEW_RESULTS_ACTION"

const queriesResultReducer = (state = [], action: StoreAction) =>{
    switch (action.type) {

        default:
            return state
    }

}

export default queriesResultReducer